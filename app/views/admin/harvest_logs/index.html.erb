<h1>Listing harvest_logs</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @harvest_logs.each do |harvest_log| %>
  <tr>
    <td><%= link_to 'Show', harvest_log %></td>
    <td><%= link_to 'Edit', edit_harvest_log_path(harvest_log) %></td>
    <td><%= link_to 'Destroy', harvest_log, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Harvest log', new_harvest_log_path %>

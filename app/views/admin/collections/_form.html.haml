= simple_form_for [:admin, @collection], :html => {:class => 'form-horizontal'} do |f|
	= f.input :name
	= f.input :full_name
	= f.input :host
	= f.input :user, input_html: { :autocomplete => :off }
	= f.input :password, input_html: { :autocomplete => :off }
	= f.button :submit
	= link_to 'Annuler', admin_collections_path, :class => 'btn'

